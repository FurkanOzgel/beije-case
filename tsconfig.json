{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "include": [
    "src/**/*",
    "declarations.d.ts" // Add this line if your declarations file is not being picked up automatically
  ]
}
